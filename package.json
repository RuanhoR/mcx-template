{
  "name": "test",
  "description": "test",
  "version": "0.0.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "i-mbler": "npm i -g mbler@latest",
    "dev": "mbler dev",
    "build": "MBLER_BUILD_MODULE=dist mbler build",
    "dev-build": "MBLER_BUILD_MODULE=dev mbler build"
  },
  "dependencies": {
    "@minecraft/server-ui": "2.0.0",
    "@mbler/mcx": "0.0.2-beta.r3"
  }
}