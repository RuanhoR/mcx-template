{
  "name": "test",
  "description": "test",
  "version": "0.0.1",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "mbler dev",
    "build": "mcx-tsc && BUILD_MODULE=release mbler build",
    "dev-build": "mbler build"
  },
  "module": "module",
  "dependencies": {
    "@mbler/mcx": "0.0.2-beta.r7",
    "@minecraft/server": "2.0.0"
  }
}
