# mcx 模板包

## 如何使用

首先，按装 [Nodejs](https://nodejs.org) 和 Git

然后，用Nodejs的Npm安装mbler（Github: [https://github.com/RuanhoR/mbler]）

```bash
npm i -g mbler
```

然后，在你安装Git之后，克隆本仓库

```bash
git clone https://github.com/RuanhoR/mcx-template.git
```

然后，构建

```bash
mbler -c .
mbler build
```
